# Руководство по установке и настройке AR Door Viewer

## 📋 Содержание

1. [Быстрая установка](#быстрая-установка)
2. [Генерация 3D-моделей](#генерация-3d-моделей)
3. [Локальное тестирование](#локальное-тестирование)
4. [Развертывание на сервер](#развертывание-на-сервер)
5. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрая установка

### Шаг 1: Проверка файлов

Убедитесь, что у вас есть все необходимые файлы:

```
ar_door_viewer.html
assets/
├── css/ar_styles.css
├── js/
│   ├── config.js
│   ├── camera_handler.js
│   ├── ml_detector.js
│   ├── scene_manager.js
│   ├── ui_controller.js
│   ├── ar_engine.js
│   └── generate_models.js
└── models/
    ├── door_classic.glb
    ├── door_modern.glb
    └── door_glass.glb
```

### Шаг 2: Создание папок

Если папок нет, создайте их:

```bash
mkdir -p assets/css
mkdir -p assets/js
mkdir -p assets/models
```

### Шаг 3: Размещение файлов

Поместите файлы в соответствующие папки согласно структуре выше.

---

## 🎨 Генерация 3D-моделей

### Способ 1: Автоматическая генерация (рекомендуется)

#### Для Windows (PowerShell):

```powershell
# Откройте PowerShell в папке проекта
# Запустите локальный сервер
python -m http.server 8000

# В браузере откройте: http://localhost:8000/ar_door_viewer.html
# Откройте консоль браузера (F12)
# Выполните команду:
generateDoorModels()

# Модели будут скачаны в папку Downloads
# Переместите их в assets/models/
```

#### Для macOS/Linux:

```bash
# Откройте терминал в папке проекта
python3 -m http.server 8000

# В браузере откройте: http://localhost:8000/ar_door_viewer.html
# Откройте консоль браузера (F12)
# Выполните команду:
generateDoorModels()

# Модели будут скачаны
# Переместите их в assets/models/
```

### Способ 2: Использование готовых моделей

Если автоматическая генерация не работает, используйте готовые модели:

1. **Скачайте модели** с сайтов:
   - [Sketchfab](https://sketchfab.com/search?q=door&type=models)
   - [Poly Haven](https://polyhaven.com/models)
   - [Free3D](https://free3d.com/3d-models/door)

2. **Конвертируйте в GLB** (если нужно):
   - Используйте [Babylon.js Sandbox](https://sandbox.babylonjs.com/)
   - Или [Three.js Editor](https://threejs.org/editor/)

3. **Поместите в папку** `assets/models/`

4. **Обновите конфиг** в `assets/js/config.js`:

```javascript
CONFIG.MODELS.available = [
    {
        id: 'door_classic',
        name: 'Классическая дверь',
        file: 'door_classic.glb',
        scale: 1.0,
        icon: '🚪'
    },
    {
        id: 'door_modern',
        name: 'Современная дверь',
        file: 'door_modern.glb',
        scale: 1.0,
        icon: '🚪'
    },
    {
        id: 'door_glass',
        name: 'Стеклянная дверь',
        file: 'door_glass.glb',
        scale: 1.0,
        icon: '🚪'
    }
];
```

### Способ 3: Создание моделей в Blender

#### Создание простой двери:

1. **Откройте Blender**
2. **Удалите куб по умолчанию** (X → Delete)
3. **Добавьте куб** (Shift+A → Mesh → Cube)
4. **Масштабируйте** (S → X → 0.5 для ширины)
5. **Масштабируйте высоту** (S → Z → 2 для высоты)
6. **Добавьте материал** (Shading → New Material)
7. **Экспортируйте в GLB**:
   - File → Export As → glTF Binary (.glb)
   - Выберите папку `assets/models/`
   - Нажмите Export glTF 2.0

#### Требования к модели:

- ✅ Высота двери: 2.0 единицы
- ✅ Ширина двери: 0.9-1.0 единицы
- ✅ Толщина: 0.05-0.1 единицы
- ✅ Полигоны: < 50,000 треугольников
- ✅ Текстуры: < 2048x2048 пикселей
- ✅ Ориентация: вдоль оси Z

---

## 🧪 Локальное тестирование

### Способ 1: Python HTTP Server

#### Windows:

```bash
# Откройте Command Prompt в папке проекта
python -m http.server 8000

# Откройте браузер: http://localhost:8000/ar_door_viewer.html
```

#### macOS/Linux:

```bash
# Откройте терминал в папке проекта
python3 -m http.server 8000

# Откройте браузер: http://localhost:8000/ar_door_viewer.html
```

### Способ 2: Node.js HTTP Server

```bash
# Установите http-server (если не установлен)
npm install -g http-server

# Запустите сервер
http-server

# Откройте браузер: http://localhost:8080/ar_door_viewer.html
```

### Способ 3: VS Code Live Server

1. **Установите расширение** "Live Server" (Ritwick Dey)
2. **Откройте файл** `ar_door_viewer.html`
3. **Нажмите** "Go Live" в правом нижнем углу
4. **Браузер откроется** автоматически

### Способ 4: Docker

```bash
# Создайте Dockerfile
cat > Dockerfile << EOF
FROM python:3.9-slim
WORKDIR /app
COPY . .
EXPOSE 8000
CMD ["python", "-m", "http.server", "8000"]
EOF

# Постройте образ
docker build -t ar-door-viewer .

# Запустите контейнер
docker run -p 8000:8000 ar-door-viewer

# Откройте браузер: http://localhost:8000/ar_door_viewer.html
```

### Тестирование на мобильном устройстве

1. **Найдите IP адрес компьютера**:
   - Windows: `ipconfig` (ищите IPv4 Address)
   - macOS/Linux: `ifconfig` (ищите inet)

2. **На мобильном устройстве** откройте:
   ```
   http://[IP_ADDRESS]:8000/ar_door_viewer.html
   ```

3. **Пример**:
   ```
   http://192.168.1.100:8000/ar_door_viewer.html
   ```

---

## 🌐 Развертывание на сервер

### Способ 1: GitHub Pages

1. **Создайте репозиторий** на GitHub
2. **Загрузите файлы**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/username/ar-door-viewer.git
   git push -u origin main
   ```

3. **Включите GitHub Pages**:
   - Settings → Pages
   - Source: main branch
   - Save

4. **Откройте**: `https://username.github.io/ar-door-viewer/ar_door_viewer.html`

### Способ 2: Netlify

1. **Создайте аккаунт** на [Netlify](https://netlify.com)
2. **Перетащите папку проекта** в Netlify
3. **Или используйте CLI**:
   ```bash
   npm install -g netlify-cli
   netlify deploy
   ```

### Способ 3: Vercel

1. **Создайте аккаунт** на [Vercel](https://vercel.com)
2. **Импортируйте проект** из GitHub
3. **Или используйте CLI**:
   ```bash
   npm install -g vercel
   vercel
   ```

### Способ 4: Собственный сервер

#### Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /var/www/ar-door-viewer;
        index ar_door_viewer.html;
        try_files $uri $uri/ =404;
    }

    # HTTPS (рекомендуется)
    listen 443 ssl;
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
}
```

#### Apache:

```apache
<VirtualHost *:80>
    ServerName your-domain.com
    DocumentRoot /var/www/ar-door-viewer

    <Directory /var/www/ar-door-viewer>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # HTTPS (рекомендуется)
    SSLEngine on
    SSLCertificateFile /path/to/cert.pem
    SSLCertificateKeyFile /path/to/key.pem
</VirtualHost>
```

---

## 🔧 Решение проблем

### Проблема: "Модели не загружаются"

**Решение:**
1. Проверьте, что файлы находятся в `assets/models/`
2. Убедитесь, что имена файлов совпадают с конфигом
3. Проверьте консоль браузера (F12) на ошибки
4. Убедитесь, что сервер работает на HTTPS (для мобильных)

### Проблема: "Камера не работает"

**Решение:**
1. Разрешите доступ к камере в настройках браузера
2. Проверьте, что браузер поддерживает WebRTC
3. Убедитесь, что используется HTTPS (для мобильных)
4. Перезагрузите страницу
5. Попробуйте другой браузер

### Проблема: "ML модель не загружается"

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что браузер поддерживает WebGL
3. Проверьте консоль на ошибки
4. Очистите кэш браузера (Ctrl+Shift+Delete)
5. Попробуйте приватный режим браузера

### Проблема: "Низкий FPS"

**Решение:**
1. Уменьшите качество в настройках (50-75%)
2. Закройте другие приложения
3. Используйте более мощное устройство
4. Отключите тени в настройках
5. Уменьшите разрешение камеры в конфиге

### Проблема: "Проем не распознается"

**Решение:**
1. Улучшите освещение
2. Отойдите дальше от проема (1-3 метра)
3. Наводите камеру перпендикулярно проему
4. Убедитесь, что проем имеет четкие границы
5. Попробуйте другой дверной проем

### Проблема: "CORS ошибка"

**Решение:**
1. Используйте HTTPS вместо HTTP
2. Убедитесь, что сервер правильно настроен
3. Проверьте заголовки CORS на сервере
4. Используйте локальный сервер для тестирования

### Проблема: "Ошибка при экспорте моделей"

**Решение:**
1. Убедитесь, что Three.js загружен
2. Проверьте консоль на ошибки
3. Используйте другой браузер
4. Скачайте готовые модели вместо генерации

---

## 📊 Проверка установки

### Чек-лист

- [ ] Все файлы на месте
- [ ] Папка `assets/models/` содержит GLB файлы
- [ ] Локальный сервер запущен
- [ ] Браузер открывает `ar_door_viewer.html`
- [ ] Нет ошибок в консоли (F12)
- [ ] Кнопка "Запустить AR" работает
- [ ] Камера запрашивает разрешение
- [ ] ML модель загружается
- [ ] 3D модель отображается
- [ ] Галерея моделей работает

### Команды для отладки

```javascript
// В консоли браузера (F12)

// Проверить конфиг
console.log(CONFIG);

// Проверить статус AR Engine
console.log(arEngine.getStatus());

// Проверить производительность
console.log(arEngine.getPerformanceInfo());

// Проверить камеру
console.log(cameraHandler.getStreamInfo());

// Проверить ML модель
console.log(mlDetector.getModelInfo());

// Проверить сцену
console.log(sceneManager.getSceneInfo());

// Включить логирование
CONFIG.DEBUG.logging = true;
```

---

## 🎓 Дополнительные ресурсы

### Документация

- [AR Door Viewer README](AR_DOOR_VIEWER_README.md)
- [Архитектурный план](plans/ar_door_visualization_plan.md)
- [Техническая спецификация](plans/ar_door_tech_spec.md)

### Полезные ссылки

- [TensorFlow.js](https://www.tensorflow.org/js)
- [Three.js](https://threejs.org/)
- [WebRTC API](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)
- [Blender](https://www.blender.org/)
- [Babylon.js Sandbox](https://sandbox.babylonjs.com/)

### Видео-туториалы

- [Three.js Basics](https://www.youtube.com/watch?v=xJAfLdlAoK8)
- [TensorFlow.js](https://www.youtube.com/watch?v=Qt3ZABW5lD0)
- [WebRTC](https://www.youtube.com/watch?v=FExZvpVvYxA)
- [Blender Door Modeling](https://www.youtube.com/watch?v=nIoXOplUvAw)

---

## ✅ Готово!

Если все прошло успешно, приложение готово к использованию! 🎉

**Следующие шаги:**
1. Откройте `ar_door_viewer.html` в браузере
2. Нажмите "Запустить AR"
3. Разрешите доступ к камере
4. Наведите на дверной проем
5. Выбирайте модели из галереи

Спасибо за использование AR Door Viewer! 🚪✨

# Структура проекта "Морской бой" 🚢

## Обзор файлов

```
battleship/
├── battleship.py              # Основной файл игры
├── config.py                  # Конфигурация параметров игры
├── test_battleship.py         # Тесты для проверки логики
├── README.md                  # Полная документация проекта
├── QUICKSTART.md              # Быстрый старт и примеры
├── PROJECT_STRUCTURE.md       # Этот файл
└── plans/
    └── battleship_plan.md     # План разработки и архитектура
```

## Описание файлов

### 📄 [`battleship.py`](battleship.py)
**Основной файл игры**

Содержит полную реализацию игры "Морской бой" с тремя основными классами:

#### Класс `Ship`
Представляет отдельный корабль на игровом поле.

**Атрибуты:**
- `size` - размер корабля (1, 2 или 3)
- `positions` - список координат, занимаемых кораблем
- `hits` - множество координат попаданий

**Методы:**
- `place(x, y, horizontal)` - размещает корабль на поле
- `hit(x, y)` - регистрирует попадание в корабль
- `is_sunk()` - проверяет, потоплен ли корабль
- `contains(x, y)` - проверяет наличие координаты в корабле

#### Класс `Board`
Управляет игровым полем размером 6x6.

**Атрибуты:**
- `SIZE` - размер поля (6)
- `SHIP_SIZES` - список размеров кораблей [3, 2, 2, 1, 1, 1]
- `ships` - список всех кораблей на поле
- `shots` - множество координат выстрелов

**Методы:**
- `can_place_ship(size, x, y, horizontal)` - проверяет возможность размещения
- `place_ship(ship_index, x, y, horizontal)` - размещает корабль
- `auto_place_ships()` - автоматически расставляет все корабли
- `shoot(x, y)` - обрабатывает выстрел и возвращает результат
- `all_ships_sunk()` - проверяет, все ли корабли потоплены
- `display(hide_ships)` - отображает поле в консоли
- `get_ship_count()` - возвращает количество оставшихся кораблей

#### Класс `Game`
Управляет игровым процессом и взаимодействием между игроком и компьютером.

**Атрибуты:**
- `player_board` - игровое поле игрока
- `computer_board` - игровое поле компьютера
- `current_turn` - чей сейчас ход ("player" или "computer")
- `game_over` - флаг окончания игры
- `winner` - победитель ("player" или "computer")

**Методы:**
- `setup()` - инициализирует игру и расставляет корабли
- `parse_coordinates(coord_str)` - парсит координаты из строки (например, "A1")
- `player_turn()` - обрабатывает ход игрока
- `computer_turn()` - обрабатывает ход компьютера (простой AI)
- `display_status()` - отображает текущий статус игры
- `play()` - основной игровой цикл

#### Функция `main()`
Точка входа в программу. Создает экземпляр игры и запускает её.

**Использование:**
```bash
python battleship.py
```

---

### ⚙️ [`config.py`](config.py)
**Конфигурация параметров игры**

Содержит все настраиваемые параметры игры в одном месте для удобства изменения.

**Параметры:**
- `BOARD_SIZE` - размер игрового поля (по умолчанию 6)
- `SHIP_SIZES` - размеры кораблей (по умолчанию [3, 2, 2, 1, 1, 1])
- `MAX_PLACEMENT_ATTEMPTS` - максимальное количество попыток размещения корабля
- `AI_TYPE` - тип AI компьютера ("random" или "smart")
- `SYMBOLS` - словарь символов для отображения
- `MESSAGES` - словарь сообщений игры
- `DEBUG_SHOW_ENEMY_SHIPS` - показывать ли корабли противника (для отладки)
- `DEBUG_VERBOSE` - показывать ли дополнительную информацию

**Функции:**
- `get_board_size()` - возвращает размер поля
- `get_ship_sizes()` - возвращает список размеров кораблей
- `get_total_ship_cells()` - возвращает общее количество клеток кораблей
- `get_ship_count()` - возвращает количество кораблей
- `get_message(key, default)` - возвращает сообщение по ключу
- `get_symbol(key, default)` - возвращает символ по ключу
- `validate_config()` - проверяет корректность конфигурации

**Использование:**
```bash
python config.py  # Проверить конфигурацию
```

---

### 🧪 [`test_battleship.py`](test_battleship.py)
**Тесты для проверки логики игры**

Содержит набор тестов для проверки корректности основных компонентов игры без интерактивного ввода.

**Тесты:**
1. `test_ship()` - тестирует класс Ship
2. `test_board()` - тестирует класс Board
3. `test_board_auto_placement()` - тестирует автоматическую расстановку кораблей
4. `test_shooting()` - тестирует логику выстрелов
5. `test_game_parsing()` - тестирует парсинг координат
6. `test_board_display()` - тестирует отображение поля

**Использование:**
```bash
python test_battleship.py
```

**Ожидаемый результат:**
```
╔════════════════════════════════════════════════╗
║     ТЕСТИРОВАНИЕ ИГРЫ 'МОРСКОЙ БОЙ'           ║
╚════════════════════════════════════════════════╝

[Результаты тестов...]

==================================================
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО! 🎉
==================================================
```

---

### 📖 [`README.md`](README.md)
**Полная документация проекта**

Содержит:
- Описание игры и её особенностей
- Правила игры
- Инструкции по установке и запуску
- Описание классов и функций
- Примеры использования
- Возможные улучшения

---

### 🚀 [`QUICKSTART.md`](QUICKSTART.md)
**Быстрый старт и примеры**

Содержит:
- Инструкции по быстрому запуску
- Примеры ввода координат
- Описание игровых символов
- Пример игровой сессии
- Советы по игре
- Решение проблем

---

### 📋 [`plans/battleship_plan.md`](plans/battleship_plan.md)
**План разработки и архитектура**

Содержит:
- Обзор проекта
- Параметры игры
- Архитектуру проекта
- Диаграмму игрового процесса
- Представление данных
- Детальный план реализации
- Технические детали
- Возможные улучшения

---

### 📁 [`PROJECT_STRUCTURE.md`](PROJECT_STRUCTURE.md)
**Этот файл**

Описание структуры проекта и назначение каждого файла.

---

## Быстрая навигация

### Для новичков
1. Начните с [`QUICKSTART.md`](QUICKSTART.md) для быстрого старта
2. Запустите игру: `python battleship.py`
3. Прочитайте [`README.md`](README.md) для полной информации

### Для разработчиков
1. Изучите [`plans/battleship_plan.md`](plans/battleship_plan.md) для понимания архитектуры
2. Посмотрите [`battleship.py`](battleship.py) для изучения кода
3. Запустите тесты: `python test_battleship.py`
4. Измените параметры в [`config.py`](config.py) при необходимости

### Для модификации
1. Измените параметры в [`config.py`](config.py)
2. Добавьте новые функции в [`battleship.py`](battleship.py)
3. Добавьте тесты в [`test_battleship.py`](test_battleship.py)
4. Обновите документацию в [`README.md`](README.md)

---

## Статистика проекта

| Метрика | Значение |
|---------|----------|
| Основной файл | `battleship.py` |
| Строк кода | ~450 |
| Классов | 3 (Ship, Board, Game) |
| Методов | ~20 |
| Тестов | 6 |
| Документация | 4 файла |
| Язык | Python 3.6+ |

---

## Зависимости

- **Python 3.6+** - основной язык
- **random** - встроенный модуль для случайных чисел
- **sys** - встроенный модуль для работы с системой
- **typing** - встроенный модуль для аннотаций типов

Все зависимости встроены в Python, дополнительная установка не требуется.

---

## Лицензия

Этот проект создан в образовательных целях.

---

## Контакты и поддержка

Для вопросов и предложений по улучшению проекта обратитесь к разработчику.
